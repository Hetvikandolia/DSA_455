#include<stdio.h>
int main(){
    int n[5]={5,5,2,2,30};
    int size=sizeof(n)/sizeof(n[0]);

    int count=0;
    int dul=0;
    for (int i = 0; i < size; i++)
    {
       if(n[i]==n[j]){count++;}

       if(count)
    }
    
}